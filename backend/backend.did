type Contact = 
 record {
   email: text;
   message: text;
   name: text;
 };
service : {
  getContacts: () -> (vec Contact) query;
  getRandomNumbers: (nat) -> (vec nat);
  submitContact: (text, text, text) -> ();
}
